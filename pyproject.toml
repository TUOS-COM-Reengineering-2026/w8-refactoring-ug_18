[project]
name = "w8-refactoring-ug-18"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.3",
    "pytest-coverage>=0.0",
]
